<!--pages/headline.wxml-->
<view class="content">
  <block wx:for="{{news}}" wx:key>
    <view class="item">
      <image class="banner" src="{{item.banner}}"></image>
      <text class="title">{{item.title}}</text>
    </view>
  </block>
</view>
